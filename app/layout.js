export const metadata = {
  title: "Video → Hooks & Caption",
  description: "Upload a video and generate hooks and captions automatically.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
